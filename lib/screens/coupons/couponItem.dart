class CouponItem {
  int durationInMonths = 0;
  double percentReduction = 0.0;
  String id, name, duration = '';

  CouponItem(this.id, this.name, this.percentReduction, this.durationInMonths,
      this.duration);
}
