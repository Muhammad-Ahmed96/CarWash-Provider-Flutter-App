class UserItem {
  int id, postalCode = 0;
  String firstname, lastname, email, telephone, address, city = '';

  UserItem(this.id, this.firstname, this.lastname, this.email, this.telephone,
      this.address, this.city, this.postalCode);
}
