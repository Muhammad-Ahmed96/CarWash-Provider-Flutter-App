class ServiceItem {
  String title, description, duration, price = '';
  int id = 0;
  int companyId = 0;

  ServiceItem(this.title, this.description, this.duration, this.price, this.id,
      this.companyId);
}
