class WasherItem {
  int id = 0;
  String name, email, phone, address, city, postalCode = '';

  WasherItem(this.id, this.name, this.email, this.phone, this.address,
      this.city, this.postalCode);
}
