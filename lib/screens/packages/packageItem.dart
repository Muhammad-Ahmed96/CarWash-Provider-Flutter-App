class PackageItem {
  String title, description, duration, price = '';
  int id = 0;
  int companyId = 0;

  PackageItem(this.title, this.description, this.duration, this.price, this.id,
      this.companyId);
}
